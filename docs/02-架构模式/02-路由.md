---
title: 路由
description: Routing 模式——根据输入特征智能分发到不同处理路径
tags: [架构模式, 路由, 工作流]
category: 02-架构模式
difficulty: 初级
created: 2026-04-27
updated: 2026-04-27
---

# 路由（Routing）

## 定义

**路由（Routing）** 是根据输入的特征（内容、类型、意图等），将请求分发到最合适的处理路径或专用模型的模式。

```mermaid
graph TD
    A[用户输入] --> B[分类器/路由器]
    B -->|类型A| C[处理路径A]
    B -->|类型B| D[处理路径B]
    B -->|类型C| E[处理路径C]
    C --> F[输出]
    D --> F
    E --> F
```

## 适用场景

- 系统需要处理多种不同类型的任务
- 不同任务类型需要不同的处理方式或专用模型
- 需要按领域/复杂度/敏感度分流
- 希望优化成本和性能（简单任务用轻量模型）

## 典型示例：客服系统

```mermaid
graph TD
    A[用户问题] --> B[意图分类]
    B -->|订单查询| C[订单处理模块]
    B -->|技术支持| D[技术支持模块]
    B -->|投诉建议| E[投诉处理模块]
    B -->|闲聊| F[闲聊回复模块]
    C --> G[回复用户]
    D --> G
    E --> G
    F --> G
```

## 代码示例

### 基于 LLM 的分类路由

```python
from enum import Enum

class QueryType(Enum):
    ORDER = "订单查询"
    TECH = "技术支持"
    COMPLAINT = "投诉建议"
    CHAT = "闲聊"

def classify_query(user_input: str) -> QueryType:
    """使用 LLM 对输入进行分类"""
    prompt = f"""请将用户输入分类为以下类型之一：
- 订单查询
- 技术支持
- 投诉建议
- 闲聊

用户输入：{user_input}

只返回类型名称，不要其他解释。"""
    
    result = llm.invoke(prompt).strip()
    return QueryType(result)

def route_and_process(user_input: str) -> str:
    query_type = classify_query(user_input)
    
    handlers = {
        QueryType.ORDER: handle_order,
        QueryType.TECH: handle_tech,
        QueryType.COMPLAINT: handle_complaint,
        QueryType.CHAT: handle_chat,
    }
    
    handler = handlers[query_type]
    return handler(user_input)
```

### 多模型路由

```python
def smart_route(prompt: str) -> str:
    """根据复杂度选择模型"""
    complexity = assess_complexity(prompt)
    
    if complexity == "simple":
        return cheap_model.invoke(prompt)  # 轻量模型，成本低
    elif complexity == "medium":
        return standard_model.invoke(prompt)
    else:
        return powerful_model.invoke(prompt)  # 强模型，成本高
```

## 变体：级联路由

```mermaid
graph TD
    A[输入] --> B[粗分类]
    B -->|技术问题| C[细分类]
    C -->|前端| D[前端专家]
    C -->|后端| E[后端专家]
    C -->|DevOps| F[运维专家]
    B -->|非技术| G[通用处理]
```

## 优缺点

| 优点 | 缺点 |
|------|------|
| 各路径可独立优化 | 分类错误会导致整个请求处理失败 |
| 可为不同任务选择最适合的模型 | 增加一层延迟（分类步骤） |
| 便于扩展新类型 | 分类边界模糊时效果下降 |
| 成本优化（简单任务用轻量模型） | 需要维护分类器和多条路径 |

## 反模式与修复

| 反模式 | 问题 | 影响 | 修复方案 |
|--------|------|------|----------|
| 无兜底（Fallback）路由 | 分类器无法识别输入类型时直接抛异常或返回空结果 | 用户请求被丢弃，产生大量未处理的边缘 case | 始终设置一条默认/兜底路径，将无法分类的请求交给通用处理模块 |
| 路由分支过多（> 10 条） | 路由条目膨胀导致分类器准确率下降，类别间边界模糊 | 分类错误率上升，维护成本高，新增路由困难 | 合并语义相近的路由，采用级联路由（先粗分类再细分类）降低单层分类压力 |
| 分类器过于复杂 | 在分类器中引入多轮推理、工具调用等复杂逻辑 | 分类步骤延迟过高，分类器本身出错率增加，"分类器变成了 Agent" | 分类器应保持简单——使用轻量模型或规则引擎，复杂逻辑留给各路由分支处理 |
| 忽略分类置信度 | 分类器返回低置信度结果时仍强制路由到某一分支 | 大量请求被错误路由，用户得到答非所问的回复 | 设置置信度阈值，低置信度时降级到通用处理或转人工 |
| 路由分支间能力重叠 | 多条路由处理同一类请求，逻辑重复且行为不一致 | 维护成本翻倍，同类型请求在不同路由下得到不同质量的回复 | 明确每条路由的职责边界，合并重叠分支，通过分类器 prompt 示例消除歧义 |

## 权衡分析

路由模式的核心设计选择是**如何分类请求以及分几条路径**。

### 分类精度 vs 延迟开销

| 维度 | LLM 分类 | 规则引擎 | 嵌入向量相似度 |
|------|----------|----------|---------------|
| 准确率 | 高（理解语义） | 中（关键词匹配） | 中高（语义相近） |
| 延迟 | 高（一次 LLM 调用） | 极低（微秒级） | 低（向量计算） |
| 成本 | 高（Token 消耗） | 无 | 低（一次嵌入） |
| 维护成本 | 低（改 Prompt） | 高（规则膨胀） | 中（需标注数据） |
| 适用场景 | 类别多、边界模糊 | 类别少、规则明确 | 有历史数据可训练 |

### 路由粒度的权衡

- **粗粒度路由（2-4 条路径）**：分类器准确率高，但每条路径需要处理的任务类型多，内部逻辑复杂
- **细粒度路由（10+ 条路径）**：每条路径高度专业化，但分类器准确率下降，类别间边界模糊
- **级联路由**是折中方案：先粗分类（准确率高），再细分类（专业化），但增加一层延迟

### 成本优化的取舍

- **多模型路由**（简单任务用轻量模型）可节省 60-80% 成本，但需要可靠的复杂度评估器
- 复杂度评估器本身可能出错——错误地将复杂任务分配给轻量模型会导致质量严重下降
- **保守策略**：宁可用强模型处理简单任务，也不要用轻量模型处理复杂任务

### 何时选择路由

- 系统需要处理**3 种以上**明显不同的任务类型
- 不同任务需要**不同的处理策略或模型**
- 需要**按成本/质量**分级处理

### 何时避免路由

- 任务类型**少于 3 种**——直接用 if-else 或提示链更简单
- 分类边界**极其模糊**——考虑让 LLM 自主判断（ReAct 模式）
- 路径之间**需要共享上下文**——编排器-工作者模式更合适

## 最佳实践

1. **分类器要简单**：分类任务本身应该是简单的，避免"分类器也变成 Agent"
2. **兜底路径**： always 提供默认/兜底处理路径
3. **持续优化**：收集分类错误样本，迭代优化分类器
4. **置信度阈值**：当分类置信度低时，降级到通用处理或人工介入

## 与其他模式的关系

- **vs [[01-提示链|提示链]]**：路由是多分支选择，提示链是单线执行
- **vs [[03-并行化|并行化]]**：路由选择一条路径，并行化同时走多条路径
- **vs [[04-编排器-工作者|编排器-工作者]]**：路由是静态分类，编排器动态分解任务

## 延伸阅读

- [[00-模式总览]] — 所有架构模式对比
- [[01-提示链]] — 单一路径的线性处理
- [[03-并行化]] — 同时尝试多条路径
